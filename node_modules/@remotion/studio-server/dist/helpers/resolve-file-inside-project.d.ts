export declare const resolveFileInsideProject: ({ remotionRoot, fileName, action, }: {
    remotionRoot: string;
    fileName: string;
    action: string;
}) => {
    absolutePath: string;
    fileRelativeToRoot: string;
};
